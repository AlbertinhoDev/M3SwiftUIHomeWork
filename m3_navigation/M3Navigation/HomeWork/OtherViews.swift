import SwiftUI

struct LoginView: View {

//    @Binding var tabSelection: Int

    let text: String
    var body: some View {
        Text(text)
    }
}

struct CatalogView: View {
    var body: some View {
        Text("Catalog")
    }
}
